<section>
  <div class="faq_section">
    <div class="faq_section_inner">
      <div class="container">
        <div class="section_header text-center">
          {% if section.settings.title != blank %}
            <h2 class="position-relative">{{ section.settings.title }}</h2>
          {% endif %}
          {% if section.settings.richtext != blank %}
            <div class="richtext">{{ section.settings.richtext }}</div>
          {% endif %}
        </div>
        <div class="product_info_list faq_section_start mt-5">
          <ul class="list-unstyled">
          	{% for block in section.blocks %}
              {% if block.settings.title != blank %}	
              <li class="mb-3">
                  <div class="product_info_title d-flex align-items-center justify-content-between">
                    <h4 class="mb-0">{{ block.settings.title }}</h4>
                    <i class="fas fa-chevron-down"></i>
                  </div>
                  {% if block.settings.richtext != blank %}
                    <div class="product_info_section section_hide">
                      <div class="richtext">
                        {{ block.settings.richtext }}
                      </div>
                    </div>
                  {% endif %}
              </li>
              {% endif %}            
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
  {
    "name": "FAQ section",
	"class": "section_start",
    "settings": [
	  {
        "type": "header",
        "content": "title and info"
      },
	  {
        "type": "text",
        "id": "title",
        "label": "Title"
      },
	  {
        "type": "richtext",
        "id": "richtext",
        "label": "Info"
      }
    ],
	"blocks":[
		{
			"type":"FAQ",
			"name":"FAQ",
			"settings":[
				{
                  "type": "text",
                  "id": "title",
                  "label": "FAQ title"
                },
				{
                  "type": "html",
                  "id": "richtext",
                  "label": "FAQ info"
                }
			]
		}
	]
  }
{% endschema %}